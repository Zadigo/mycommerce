* [ ] Session ID should be JWT token in order to be able to verify its authenticity and prevent malicious users from creating payment intents with random session IDs. The JWT token should contain the session ID and any other relevant information that we might need to create the payment intent. We can then verify the JWT token using a secret key and extract the session ID from it to create the payment intent.
