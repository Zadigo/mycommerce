[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -v -s --cov=ecommercemcp --cov-report=term-missing
markers =
    slow: mark a test as slow.
    integration: mark a test as an integration test.
    unit: mark a test as a unit test.

