import 'package:flutter/material.dart';

class AccountIndexScreen extends StatelessWidget {
  const AccountIndexScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text('Account Index Page'),
      );
  }
}
  