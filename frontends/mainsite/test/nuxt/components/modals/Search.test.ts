import { describe, it, expect } from 'vitest'
import SearchModal from '~/components/modals/Search.vue'

describe('components > modals > search', () => {
  it('simple test', () => {
    expect(true).toBeTruthy()
  })
})
