import { describe, it, expect } from 'vitest'
import CartModal from '~/components/modals/Cart.vue'

describe('components > modals > cart', () => {
  it('simple test', () => {
    expect(true).toBeTruthy()
  })
})
