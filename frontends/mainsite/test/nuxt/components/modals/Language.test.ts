import { describe, it, expect } from 'vitest'
import LanguageModal from '~/components/modals/Language.vue'

describe('components > modals > language', () => {
  it('simple test', () => {
    expect(true).toBeTruthy()
  })
})
