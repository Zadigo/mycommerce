import { describe, it, expect } from 'vitest'
import DeliveryModal from '~/components/modals/Delivery.vue'

describe('components > modals > delivery', () => {
  it('simple test', () => {
    expect(true).toBeTruthy()
  })
})
